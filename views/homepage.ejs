<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Homepage</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Danh sách Users</h1>
        
        <% if (typeof data !== 'undefined' && data) { %>
            <% let users = JSON.parse(data); %>
            <% if (users.length > 0) { %>
                <table>
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Email</th>
                            <th>First Name</th>
                            <th>Last Name</th>
                            <th>Address</th>
                            <th>Phone Number</th>
                            <th>Gender</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% users.forEach(function(user) { %>
                            <tr>
                                <td><%= user.id %></td>
                                <td><%= user.email %></td>
                                <td><%= user.firstName %></td>
                                <td><%= user.lastName %></td>
                                <td><%= user.address %></td>
                                <td><%= user.phoneNumber %></td>
                                <td><%= user.gender ? 'Nam' : 'Nữ' %></td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            <% } else { %>
                <p>Không có user nào trong database.</p>
            <% } %>
        <% } else { %>
            <p>Không có dữ liệu để hiển thị.</p>
        <% } %>
        
        <br>
        <a href="/crud">Thêm User Mới</a>
        <br>
        <a href="/get-crud">Xem Danh Sách User (CRUD)</a>
    </div>
</body>
</html>
